package com.example.weatheractivity;

import com.example.weatheractivity.models.City;
import com.example.weatheractivity.models.Location;

public interface ICityServiceListener {

     void onCitySuccess (City city);
}
